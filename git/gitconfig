[core]
	editor = vim
[merge]
	tool = vimdiff
[diff]
	color = true
[color]
	diff = auto
	status = auto
	branch = auto
[alias]
	up = "!git fetch --all --recurse-submodules -p; git merge --ff-only @{u}"
	compact = gc --prune --aggressive
	commit = commit -S
	clone = clone --recursive
